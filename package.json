{
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "dev": "lerna run dev",
    "dev-web": "lerna run dev --scope=client",
    "dev-server": "lerna run dev --scope=server",
    "dev-contracts": "lerna run dev --scope=smart-contracts",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  },
  "name": "cryptagram"
}
